# Variables de déploiement production (derrière le proxy partagé) — à copier :
#   cp .env.prod.dist .env.prod
#   docker compose --env-file .env.prod -f compose.proxy.yaml up -d --build

# Secret de signature des sessions admin (JWT) — générer avec :
#   openssl rand -hex 32
LUDO_JWT_SECRET=change_moi_avec_un_secret_aleatoire_long
